<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Android 12+ (API 31) system splash screen.

        On API 31 and above the platform draws the splash itself and IGNORES the
        android:background="@drawable/splash" set in values/styles.xml, so the
        launch screen fell back to the platform default (a desaturated blue grey)
        and showed the adaptive launcher icon rather than splash.png. The two
        attributes below are the ones the platform actually reads.

        The background deliberately reuses @color/ic_launcher_background (#272D34)
        so the splash and the adaptive icon's background layer are the same colour
        and the icon appears to sit on a continuous field.

        postSplashScreenTheme is NOT set here. It is a real gap — without it the
        activity stays on Theme.SplashScreen after the splash finishes instead of
        moving to AppTheme.NoActionBar — but it is a behavioural change unrelated
        to the launcher icon, and this release is being tested on device for the
        icon. Tracked separately.

        drawable*/splash.png stays in place for pre-31 devices, which still use the
        values/styles.xml path.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
    </style>
</resources>
