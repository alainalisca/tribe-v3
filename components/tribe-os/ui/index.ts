/**
 * Tribe.OS UI primitives — barrel export.
 *
 * Import with:
 *   import { Button, Card, Badge, Avatar, Input, StatCard, DataTable, EmptyState } from '@/components/tribe-os/ui';
 *
 * Namespaced under components/tribe-os/ui/ so it doesn't collide with
 * the consumer-app shadcn primitives at components/ui/.
 */

export { default as Avatar } from './avatar';
export { default as Badge } from './badge';
export { default as Button } from './button';
export { Card, CardContent, CardFooter, CardHeader, CardTitle } from './card';
export { default as DataTable } from './data-table';
export { default as EmptyState } from './empty-state';
export { default as Input } from './input';
export { default as StatCard } from './stat-card';
